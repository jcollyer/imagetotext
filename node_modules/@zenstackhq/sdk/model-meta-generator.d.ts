import { DataModel } from '@zenstackhq/language/ast';
import { Project } from 'ts-morph';
export type ModelMetaGeneratorOptions = {
    output: string;
    compile: boolean;
    preserveTsFiles: boolean;
    generateAttributes: boolean;
};
export declare function generate(project: Project, models: DataModel[], options: ModelMetaGeneratorOptions): Promise<void>;
