import { Model } from '@zenstackhq/language/ast';
import { PluginGlobalOptions, PluginOptions } from '@zenstackhq/sdk';
/**
 * Generates source file that contains Prisma query guard objects used for injecting database queries
 */
export default class PolicyGenerator {
    generate(model: Model, options: PluginOptions, globalOptions?: PluginGlobalOptions): Promise<void>;
    private generateAuthSelector;
    private isEnumReferenced;
    private getPolicyExpressions;
    private processUpdatePolicies;
    private hasFutureReference;
    private generateQueryGuardForModel;
    private generateReadFieldsCheckers;
    private generateReadFieldCheckerFunction;
    private generateReadFieldsOverrideGuards;
    private generateUpdateFieldsGuards;
    private canCheckCreateBasedOnInput;
    private generateSelectForRules;
    private generateQueryGuardFunction;
    private generateInputCheckFunction;
    private generateNormalizedAuthRef;
}
